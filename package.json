{
    "name": "lista-de-compras-backend",
    "version": "0.1.0",
    "description": "",
    "main": "server.js",
    "scripts": {
        "pre-run": "rmdir /s /q .\\dist",
        "test": "npm run pre-run && echo \"Error: no test specified\" && exit 1",
        "build": "npm run pre-run && tsc --project tsconfig.json && tsc-alias -p tsconfig.json",
        "start": "node dist/server.js",
        "dev": "npm run pre-run && nodemon -r tsconfig-paths/register api/server.ts"
    },
    "keywords": [],
    "author": "Pedro Figueira",
    "license": "MIT",
    "dependencies": {
        "cors": "^2.8.5",
        "dotenv": "^16.4.7",
        "express": "^4.21.2",
        "mongodb": "^6.14.2",
        "tsconfig-paths": "^4.2.0"
    },
    "devDependencies": {
        "@types/cors": "^2.8.17",
        "@types/express": "^5.0.0",
        "@types/node": "^22.13.10",
        "concurrently": "^9.1.2",
        "nodemon": "^3.1.9",
        "ts-node": "^10.9.2",
        "tsc-alias": "^1.8.11",
        "typescript": "^5.8.2"
    }
}
